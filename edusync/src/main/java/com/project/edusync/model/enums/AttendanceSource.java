package com.project.edusync.model.enums;

/**
 * Defines the source method by which an attendance record was captured.
 */
public enum AttendanceSource {
    MANUAL,
    BIOMETRIC,
    SYSTEM // e.g., automatically marked based on a timetable
}